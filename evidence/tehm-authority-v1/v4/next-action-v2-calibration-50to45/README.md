# V2 50→45 calibration evidence

This directory records three source-disjoint calibration lineages.  Each had
successful 50%/45% ORFS, independent equivalence, strict signoff, graph, and
external observation evidence.  Calibration observations were deliberately
not imported into the support learner.

- calibration observations: 3; learner-eligible imports: 0;
- contract result: 0/3 pass and 3/3 fail;
- raw Pareto harmful: 3/3;
- conformal policy: `ready`, empirical and per-metric coverage `1.0`, with
  broad uncertainty intervals;
- canonical import: 0; `promotion_attempted=false`.

The policy is usable only for bounded selector proposals.  It does not create
canonical memory or production authority.
