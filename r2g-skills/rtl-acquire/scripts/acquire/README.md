# acquire

Canonical implementations for candidate intake, repo discovery, cloning, and pre-expansion source preparation.

The migrated top-level files in `scripts/*.py` remain compatibility wrappers so existing commands do not change.
