# validate

Readiness checks, duplicate audits, drift checks, and publication gating support.

This directory now holds the canonical implementation files for the validate group.
The corresponding top-level `scripts/*.py` files are compatibility wrappers.
