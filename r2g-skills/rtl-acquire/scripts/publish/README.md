# publish

Canonical implementations for publish eligibility construction, merged manifest refresh, index rebuilds, and dataset snapshot recording.

The migrated top-level files in `scripts/*.py` remain compatibility wrappers so existing commands do not change.
