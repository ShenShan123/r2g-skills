"""Integration with the R2G runtime (design doc 20.x additive seam).

env-gated, fail-closed hooks that let TEHM participate in the REAL fix loop when
``R2G_MEMORY_BACKEND=tehm`` without changing legacy behavior otherwise.
"""
