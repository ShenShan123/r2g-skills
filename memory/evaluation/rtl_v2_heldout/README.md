# V2 held-out RTL

These six RTL sources are the source-disjoint held-out cohort for the
independent `TIMING_RELIEF_BUDGETED_V2_50_TO_45` selector preflight.  They are
not support or calibration sources and must remain outside canonical memory
until a separately authorized promotion campaign exists.
